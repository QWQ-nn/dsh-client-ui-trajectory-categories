# Changelog

## [0.2.0] - 2026-08-14

### Added
- **问题画布（one problem per canvas）**：一个画布只显示一个问题，其余问题在上方「问题画布」标签栏切换，标签带状态圆点（红 = 有报错，绿 = 已交付）。
- **点击交互全面修复**：修复 pointer-capture 导致浏览器中 click 被重定向到 wrapper、点击无响应的问题；点击逻辑移到节点 wrapper 上并增加 tapGuard（拖动后点击被忽略）。
- **点击问题 → 展开/收起全部子流程**；点击方案 → 滑入/滑出工具链（收起显示 `×N` 角标）；点击工具 → 详情面板：**做了什么 / 输出·日志 / 修改内容（旧→新）/ 参数 / 错误**。
- **松散思维导图布局**：方案胶囊上下错落（zigzag），工具节点带轻微波浪形起伏，不再一条线；节点间距加大。
- 详情面板靠近画布右缘/底缘时自动翻转到另一侧，避免被裁剪。
- 节点位置改为按需默认值（`defaultPos`），新问题（如加载更早记录后）也能正确连线与拖动。

### Changed
- 时间标尺改为按当前画布的问题起止时间显示。
- 移除问题之间的衬线分隔（改为问题画布标签切换）。

### Fixed
- 工具点击详情在真实浏览器中不显示（pointer capture 重定向 click）——已修复并有 jsdom 交互测试覆盖。
- 引导线端点与节点边缘对齐（固定节点高度：问题 74 / 方案 54 / 工具 44，中线 +37 / +27 / +22）。

## [0.1.0] - 2026-08-12

### Added
- 「分类」标签页：左上角「合计」面板（写入/读取/删除/下载/命令/搜索/子代理/其他工具/消息/压缩，点击展开记录，支持筛选）。
- 中文分类：分类标签固定中文；中英文工具名/术语均可匹配归类。
- 思维导图画布：问题 → 方案 → 工具调用 流程重建；左键拖动平移、Ctrl+滚轮缩放、悬停高亮、光标排斥、节点拖动（父带动子）、弹性引导线（惯性）。
- 纯浏览器端聚合，零模型请求、零额外 token 消耗。
